.container { min-height: 100vh; background: linear-gradient(180deg, #0f172a, #1e293b); }

/* ── 场景选择 ── */
.scene-view { padding: 32rpx 24rpx; }
.header { text-align: center; padding: 60rpx 0 40rpx; }
.title { font-size: 48rpx; color: #e2e8f0; display: block; margin-bottom: 12rpx; font-weight: bold; }
.subtitle { font-size: 26rpx; color: #94a3b8; }

.scene-grid { display: grid; grid-template-columns: 1fr 1fr; gap: 16rpx; }
.scene-card { background: rgba(255,255,255,0.05); border: 2rpx solid rgba(255,255,255,0.08); border-radius: 20rpx; padding: 24rpx 16rpx; text-align: center; }
.scene-card:active { transform: scale(0.97); background: rgba(56,189,248,0.12); border-color: #38bdf8; }
.scene-emoji { font-size: 40rpx; display: block; margin-bottom: 4rpx; }
.scene-name { font-size: 28rpx; color: #e2e8f0; display: block; margin-bottom: 4rpx; }
.scene-goal { font-size: 20rpx; color: #64748b; display: block; margin-bottom: 12rpx; }
.card-btns { display: flex; gap: 8rpx; }
.card-btn { flex: 1; padding: 10rpx 0; border-radius: 12rpx; font-size: 22rpx; border: none; text-align: center; }
.card-btn.train { background: rgba(56,189,248,0.15); color: #38bdf8; }
.card-btn.script { background: rgba(251,191,36,0.12); color: #fbbf24; }

/* ── 聊天 ── */
.chat-view { display: flex; flex-direction: column; height: 100vh; }
.chat-header { text-align: center; padding: 20rpx 24rpx; border-bottom: 1rpx solid rgba(255,255,255,0.06); }
.chat-scene { font-size: 30rpx; color: #38bdf8; display: block; }
.chat-persona { font-size: 24rpx; color: #94a3b8; display: block; margin-top: 4rpx; }

.chat-area { flex: 1; padding: 16rpx 24rpx; overflow-y: auto; }
.msg-item { margin-bottom: 20rpx; }
.msg-label { font-size: 20rpx; color: #64748b; margin-bottom: 4rpx; display: block; }
.msg-bubble { padding: 16rpx 20rpx; border-radius: 16rpx; font-size: 28rpx; line-height: 1.6; color: #e2e8f0; display: inline-block; max-width: 80%; }
.msg-bubble-bot { background: rgba(129,140,248,0.1); border: 1rpx solid rgba(129,140,248,0.15); }
.msg-bubble-user { background: rgba(56,189,248,0.1); }
.msg-user { text-align: right; }
.msg-typing { text-align: center; color: #64748b; font-size: 24rpx; padding: 16rpx; }

/* 输入区 */
.input-area { padding: 12rpx 24rpx 40rpx; border-top: 1rpx solid rgba(255,255,255,0.06); background: rgba(15,23,42,0.95); }
.input-row { display: flex; gap: 12rpx; align-items: center; margin-bottom: 12rpx; }
.chat-input { flex: 1; padding: 16rpx 20rpx; background: rgba(255,255,255,0.05); border: 1rpx solid rgba(255,255,255,0.1); border-radius: 24rpx; color: #e2e8f0; font-size: 28rpx; }
.send-btn { width: auto !important; padding: 16rpx 28rpx; background: linear-gradient(135deg, #38bdf8, #818cf8); color: #fff; font-size: 28rpx; border-radius: 24rpx; border: none; }

.chat-actions { display: flex; gap: 10rpx; flex-wrap: wrap; }
.action-btn { flex: 1; min-width: 180rpx; padding: 14rpx 10rpx; border-radius: 12rpx; font-size: 24rpx; text-align: center; background: transparent; border: 1rpx solid rgba(255,255,255,0.1); color: #94a3b8; }
.end-btn { border-color: rgba(56,189,248,0.3); color: #38bdf8; }
.skip-btn { border-color: rgba(251,191,36,0.3); color: #fbbf24; }
.back-btn { font-size: 22rpx; color: #64748b; border: none; min-width: auto; flex: 0; }

/* 加载 */
.init-loading { text-align: center; padding-top: 40vh; color: #94a3b8; font-size: 28rpx; }

/* 快速测试 */
.quick-test { margin-bottom: 24rpx; }
.test-btn { width: 100% !important; padding: 20rpx; background: rgba(255,255,255,0.06); border: 2rpx dashed rgba(255,255,255,0.15); border-radius: 16rpx; color: #94a3b8; font-size: 28rpx; }
.test-result { margin-top: 12rpx; padding: 16rpx; border-radius: 12rpx; font-size: 24rpx; text-align: center; }
.test-result.ok { background: rgba(74,222,128,0.1); color: #4ade80; }
.test-result.fail { background: rgba(248,113,113,0.1); color: #f87171; }

/* 诊断 */
.diag-area { margin-top: 40rpx; padding: 20rpx; background: rgba(255,255,255,0.02); border-radius: 16rpx; border: 1rpx solid rgba(255,255,255,0.05); }
.diag-btn { width: 100% !important; padding: 16rpx; background: rgba(255,255,255,0.04); border: 1rpx dashed rgba(255,255,255,0.12); border-radius: 12rpx; color: #64748b; font-size: 26rpx; }
.diag-results { margin-top: 16rpx; }
.diag-row { padding: 6rpx 0; font-size: 22rpx; color: #94a3b8; line-height: 1.8; }
.diag-summary { margin-top: 10rpx; padding: 10rpx; background: rgba(56,189,248,0.08); border-radius: 8rpx; color: #38bdf8; font-size: 24rpx; text-align: center; font-weight: bold; }
