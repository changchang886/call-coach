services:
  - type: web
    name: call-coach
    env: python
    buildCommand: pip install -r requirements.txt
    startCommand: python server.py
    envVars:
      - key: DEEPSEEK_API_KEY
        sync: false
